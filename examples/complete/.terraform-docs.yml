version: '>= 0.14.0'

formatter: markdown table

content: |-
  # Merge Module Example

  To run this example you need to execute:

  ```bash
  $ terraform init
  $ terraform plan
  $ terraform apply
  ```

  ```hcl
  {{ include "./main.tf" }}
  ```

output:
  file: README.md
  mode: replace
